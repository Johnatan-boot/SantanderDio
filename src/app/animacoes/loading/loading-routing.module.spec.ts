import { LoadingRoutingModule } from './loading-routing.module';

describe('LoadingRoutingModule', () => {
  it('should create an instance', () => {
    expect(new LoadingRouting.Module()).toBeTruthy();
  });
});
